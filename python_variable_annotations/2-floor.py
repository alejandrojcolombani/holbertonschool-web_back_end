#!/usr/bin/env python3
"""Provide a type-annotated function that floors a float."""

import math


def floor(n: float) -> int:
    """Return the mathematical floor of ``n`` as an integer."""
    return math.floor(n)
